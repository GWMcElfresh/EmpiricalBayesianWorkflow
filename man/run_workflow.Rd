% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/workflow.R
\name{run_workflow}
\alias{run_workflow}
\title{Run Complete Mixed Model Workflow with Flexible Family}
\usage{
run_workflow(
  data = NULL,
  formula = NULL,
  family = "zero_inflated_beta",
  zi_formula = ~1,
  n_subjects = 30,
  n_obs_per_subject = 20,
  zi_prob = 0.15,
  train_prop = 0.5,
  calib_prop = 0.25,
  inflation_factor = 2,
  conformal_method = c("split", "jackknife"),
  alpha = 0.1,
  backend = c("cmdstanr", "rstan"),
  verbose = TRUE,
  ...
)
}
\arguments{
\item{data}{Optional data frame. If NULL, synthetic data is generated.}

\item{formula}{Model formula. Required if data is provided.}

\item{family}{Family specification. Default is "zero_inflated_beta".}

\item{zi_formula}{Zero-inflation formula. Default is \code{~1}.}

\item{n_subjects}{Number of subjects for synthetic data. Default is 30.}

\item{n_obs_per_subject}{Observations per subject for synthetic data. Default is 20.}

\item{zi_prob}{Zero-inflation probability for synthetic data. Default is 0.15.}

\item{train_prop}{Proportion of data for training. Default is 0.5.}

\item{calib_prop}{Proportion of data for calibration. Default is 0.25.}

\item{inflation_factor}{Prior inflation factor. Default is 2.}

\item{conformal_method}{Conformal method: "split" or "jackknife". Default is "split".}

\item{alpha}{Miscoverage level for conformal prediction. Default is 0.1.}

\item{backend}{Stan backend: "cmdstanr" (default) or "rstan".}

\item{verbose}{Whether to print progress messages. Default is TRUE.}

\item{...}{Additional arguments passed to fitting functions.}
}
\value{
A list containing:
\item{model_freq}{Fitted frequentist model}
\item{model_bayes}{Fitted Bayesian model}
\item{conformal_results}{Conformal prediction results}
\item{data}{List of train/calibration/test data}
\item{priors}{Prior specifications}
}
\description{
Executes the complete empirical Bayesian workflow including:
\enumerate{
\item Data generation or splitting
\item Frequentist model fitting
\item Prior extraction and inflation
\item Bayesian model refitting with cmdstanr
\item Conformal prediction
}
}
\details{
Supports multiple model families with exact parameter mapping.
}
\examples{
\dontrun{
# Run with synthetic data (zero-inflated beta)
results <- run_workflow(verbose = TRUE)

# Run with different family
results <- run_workflow(family = "gamma", verbose = TRUE)

# Run with your own data
results <- run_workflow(
  data = my_data,
  formula = y ~ x1 + x2 + (1 + x1 | subject_id),
  family = "beta"
)
}
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bayesian_fitting.R
\name{fit_bayesian_model}
\alias{fit_bayesian_model}
\title{Fit Bayesian Mixed Model with Flexible Family}
\usage{
fit_bayesian_model(
  formula,
  data,
  family = "zero_inflated_beta",
  zi_formula = ~1,
  prior = NULL,
  backend = c("cmdstanr", "rstan"),
  chains = 4,
  iter = 2000,
  warmup = 1000,
  cores = 4,
  seed = 123,
  ...
)
}
\arguments{
\item{formula}{A brms formula object or standard formula.}

\item{data}{Data frame containing the variables.}

\item{family}{Family specification. Can be:
\itemize{
\item Character: "beta", "zero_inflated_beta", "gamma", etc.
\item brms family object
Default is "zero_inflated_beta".
}}

\item{zi_formula}{Formula for zero-inflation component (for ZI families).}

\item{prior}{Optional brms prior specifications.}

\item{backend}{Stan backend: "cmdstanr" (default) or "rstan".}

\item{chains}{Number of MCMC chains. Default is 4.}

\item{iter}{Total iterations per chain. Default is 2000.}

\item{warmup}{Warmup iterations per chain. Default is 1000.}

\item{cores}{Number of cores for parallel processing. Default is 4.}

\item{seed}{Random seed for reproducibility.}

\item{...}{Additional arguments passed to brms::brm.}
}
\value{
A fitted Bayesian model object.
}
\description{
Fits a mixed model using Bayesian methods (Stan/brms) with cmdstanr backend.
Supports multiple families including zero-inflation.
}
\examples{
\dontrun{
# With informative priors from frequentist model
model_freq <- fit_frequentist_model(
  y ~ x + (1 + x | subject_id),
  data = data,
  family = "zero_inflated_beta"
)
prior_list <- extract_and_inflate_priors(model_freq)
priors <- create_brms_priors(prior_list)

# Fit Bayesian model with cmdstanr
model_bayes <- fit_bayesian_model(
  y ~ x + (1 + x | subject_id),
  data = data,
  family = "zero_inflated_beta",
  prior = priors,
  backend = "cmdstanr"
)

# Different family
model_gamma <- fit_bayesian_model(
  y ~ x + (1 | subject_id),
  data = data,
  family = "gamma",
  prior = priors
)
}
}

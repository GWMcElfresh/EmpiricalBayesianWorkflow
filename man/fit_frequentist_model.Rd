% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/frequentist_fitting.R
\name{fit_frequentist_model}
\alias{fit_frequentist_model}
\title{Fit Frequentist Mixed Model with Flexible Family}
\usage{
fit_frequentist_model(
  formula,
  data,
  family = "zero_inflated_beta",
  zi_formula = ~1,
  method = c("auto", "glmmTMB"),
  ...
)
}
\arguments{
\item{formula}{Formula for the conditional model, including random effects.}

\item{data}{Data frame containing the variables.}

\item{family}{Family specification. Can be:
\itemize{
\item Character: "beta", "zero_inflated_beta", "gamma", "binomial", "poisson", etc.
\item Family object from glmmTMB or stats
Default is "zero_inflated_beta".
}}

\item{zi_formula}{Formula for the zero-inflation component. Default is \code{~1}.
Only used for zero-inflated families.}

\item{method}{Backend to use: "glmmTMB" (preferred), "auto". Default is "auto".}

\item{...}{Additional arguments passed to the backend fitting function.}
}
\value{
A fitted model object with class attribute indicating the backend and family used.
}
\description{
Fits a mixed model using a frequentist approach with support for multiple
families including zero-inflation and hurdle models.
Supports multiple backends, with glmmTMB as the preferred method.
}
\examples{
\dontrun{
# Zero-inflated beta (default)
data <- generate_zibeta_data(n_subjects = 20, n_obs_per_subject = 10)
model <- fit_frequentist_model(
  y ~ x + (1 + x | subject_id),
  data = data,
  family = "zero_inflated_beta"
)

# Regular beta
model <- fit_frequentist_model(
  y ~ x + (1 | subject_id),
  data = data,
  family = "beta"
)

# Poisson
model <- fit_frequentist_model(
  count ~ x + (1 | subject_id),
  data = count_data,
  family = "poisson"
)
}
}
